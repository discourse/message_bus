before_install: gem install bundler
language: ruby
rvm:
  - 2.0
  - 2.1.10
  - 2.2.8
  - 2.3.5
  - 2.4.2
gemfile:
  - Gemfile
addons:
  postgresql: "9.4"
env:
  - PGUSER=postgres
before_script:
  - psql -c 'create database message_bus_test;' -U postgres
services:
  - redis-server
  - postgresql
